- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config', '~/.nvm']

- link:
    ~/.dotfiles_local: ''
    ~/.gitconfig_local:
    ~/.shell_local_after:

- shell:
  - git submodule sync --recursive
