- defaults:
    link:
      create: true
      relink: true

- clean:
    '~':
        recursive: false
        force: true
    '~/.config/':
        force: true
    '~/.nvm':
        recursive: false
        force: true
    '~/.local/bin':
        recursive: false
        force: true
    '~/bin':  # Deprecated location. We're going to start preferring `~/.local/bin` for simplicity.
        recursive: true
        force: true

- link:
    ~/.dotfiles_local: ""
    ~/.config/:
      glob: true
      path: "config/*"
    ~/.gitconfig_local:
    ~/.nvm:
    ~/.shell_local_after:
    ~/.shell_local_after.ps1:
    ~/.local/bin/:
      glob: true
      path: "bin/*"
