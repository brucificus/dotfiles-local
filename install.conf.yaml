- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.dotfiles_local: ''
    ~/.gitconfig_local:
    ~/.nvm:
    ~/.shell_local_after:
    ~/.ssh:
      glob: true
      path: ssh/*

- shell:
  - git submodule sync --recursive
